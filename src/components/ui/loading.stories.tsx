import { Meta, StoryObj } from "@storybook/react";
import { LoadingSpinner } from "./loading";


export default {
  title: "Componentes/ui/loading-spinner",  // Título na Storybook
  component: LoadingSpinner,
} as Meta;

export const Default: StoryObj = {
  args: {},
};
